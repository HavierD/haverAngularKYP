export class Record {
    word: string;
    year: number;
    month: number;
    day: number;

    constructor(word: string, year: number, month: number, day: number) {
        this.word = word;
        this.year = year;
        this.month = month;
        this.day = day;
    }
}
